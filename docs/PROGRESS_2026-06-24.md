# PROGRESS 2026-06-22 ~ 06-24 — Horizon 재설계 완결 · ETF · 점검/장애 자동공지

> 기간: 2026-06-22 ~ 06-24. 전부 main 머지 + prod 라이브(백엔드 all-of-asset `stock-screener`,
> 프론트 Vercel `axis-web`/axislytics.com). 커밋 16건(`8472f95`…`2c36294`).

---

## 1. 페르소나 → 시간축 관점(Horizon) 전면 전환 (Phase 5 완결)

기존 6 페르소나(블랙록/ARK/그레이엄 + event/macro/korean)를 **4 시간축 관점(단기/단중기/중기/장기)**
으로 대체. 설계 = `docs/axis/HORIZONS.md`.

- `8472f95` **Phase 5**: 블랙록/ARK/그레이엄 폐지. 결정 = *노드 보존, UI만 제거* — event/macro/korean
  에이전트·노드는 graph에 내부 데이터 제공자로 잔존, 사용자 노출만 제거. horizon이 유일한 1차 축.
  - 백엔드: `personas/{blackrock,ark,graham}.md` 삭제 / strategist horizon 전용(`_build_full_system(horizon)`,
    빈값→mid, 실명 마스킹 guard) / graph `route_by_persona`→`route_by_horizon` / ai.py `_PERSONAS` 3종 제거·
    profile `preferred_persona`→`preferred_horizon`.
  - 프론트: PersonaChooser·PersonaSwitch·PersonaGuideModal·personaStore 삭제 / AnalyzeView horizon 전용 /
    onboarding·settings·dashboard "선호 페르소나"→"선호 시계".
- `b7d2edf`·`ca29aa9`·`83d1b4b` **잔존 문구 정리**: 랜딩/가격/개인정보/구독안내/HeroPreview 데모/이전분석·
  최근분석 카드의 페르소나 문구·이름 제거. **StrategistCard 기능 수정**(persona_used가 horizon이라 맵 미스
  → 부제 "mid" 영문·진입 철학 미표시였음 → HORIZON_NAME/HORIZON_PHILOSOPHY).
- `18e93a8` **저점·고점 "어디까지" 표현**: StrategistCard 최상단 "📐 [관점] — 어디까지?(저점 관찰구간 ~
  고점 참고선)" 밴드(현재가 대비 %, KR컬러, '예측 아닌 관찰' 명시). 랜딩 헤딩·기능카드도 반영.
- 검증: pytest 743 pass(신규 실패 0) · web tsc 클린 · 라이브 스모크(005930 short/long, 실명 마스킹·
  validator PASS) · Playwright로 전 화면(공개+로그인 게이트) 페르소나 잔존 0 확인.

## 2. 요금제 — 단기만 Free, Premium 제거, 관심종목 무제한

- `18e93a8` **Pro 게이팅**: `_HORIZONS.available_to_free` = 단기만 True, 단중기·중기·장기 Pro(402
  HORIZON_LOCKED). HorizonChooser가 Free에 잠긴 기본시계 대신 단기 자동선택.
- `3335092` **관심종목 허위 한도 제거**: "Free 5/Pro 30"은 미적용(전체 500캡)이었음 → 가격·랜딩·설정
  모두 "관심 종목 무제한"으로 정정(등록은 AI 비용 0). 대시보드 미리보기 캡도 티어 무관 통일.
- `48bec77` **Premium 잔재 제거**: `PLAN_LIMITS` premium·`_require_pro`·프론트 user_plan 유니온·
  UsageCard·pricing에서 제거. 2-tier(Free/Pro) 확정. (재도입은 추후 별도.)
- 최종 게이팅: 분석 Free 20/Pro 100, 검증 10/50, 발견 5/30, 시계 단기/4종, 스마트리스트 6/17,
  커스텀 스크리너 Pro전용, 관심종목 무제한.

## 3. 한글깨짐 근본 수정 (sector·테마명 인코딩)

- `ac60dbd` **원인**: 네이버 finance가 UTF-8로 전환됐는데 `data_fetcher`가 `euc-kr` 강제 디코딩 →
  한글(섹터·테마명) U+FFFD mojibake(숫자 PER/PBR은 ASCII라 무사). 4곳 전부 `utf-8`로. + 서빙 방어선
  `repository.load_stocks`에서 U+FFFD 포함 문자열 → "" 정화(영구 유지).
- `6198128` cloudbuild-collector CLOUD_URL 옛 프로젝트→all-of-asset(reload 콜백).
- **재수집**: collector-heavy(`--all`) @ **stock-search-program**(⚠️2-프로젝트 트랩: collector는 그쪽,
  서빙은 all-of-asset) 실행 → sector 실값 복원(005930="반도체와반도체장비" 등) + reload 콜백 end-to-end.

## 4. ETF 전용 상세 + 발견 페이지 (신규)

- `1adabe9` **ETF 상세 `/etf/{ticker}`**(KR 상장): 네이버 모바일 `etfAnalysis` API — 운용사·추적지수·
  NAV·총보수·추적오차 + 상위10 구성종목(비중바) + 섹터/국가/자산 비중. 백엔드 `api/routes/etf.py`
  GET `/api/etf/{ticker}` + Firestore 일일 캐시(etf_details). `/api/all-stocks`에 stock_type 추가.
  스크리너·검색에서 ETF면 `/etf/`로 라우팅. region 분류(국내/국내상장 국외).
- `5dc84cb` **해외(US상장) ETF**: 알파벳 티커 → yfinance funds_data(SPY/QQQ 등, 상위10·섹터·운용사·
  총자산·보수). + **ETF 발견 `/etf`**(해외 큐레이션 + 국내 거래량 상위). 대시보드 카드.
- `ff6a4ef` 네비 메뉴(사이드바+모바일)에 🧺 ETF 추가.
- `f5d22d0` 발견 페이지를 **칩 3분류**(국내 ETF / 국내상장 해외 / 해외 ETF) — etf_category + 종목명
  키워드(미국·나스닥 등)로 국내 vs 국내상장 해외 보정.
- `d1d5a1a` **버그 수정**: KR ETF 신상품(단일종목·레버리지)은 문자 포함 6자 KRX 코드(예 `0193T0`) 사용
  → `^\d{6}$` 미스로 yfinance행 404(200개 중 64개). 라우팅을 "순수 알파벳=US, 그 외=KR(naver)"로 변경.
- ⚠️ 상위 10 구성종목만(네이버/yfinance 한계). 전체 PDF는 KRX 직접(pykrx 1.2.4는 깨짐) 추후.

## 5. 점검 공지 + Claude API 장애 자동 감지

- `1e03d8f` **점검/이용 제한 공지 배너**(비차단): 관리자 콘솔(/admin '점검 공지' 탭)에서 켜기·시작/종료·
  메시지 설정 → 전 사용자 상단 배너(루트 레이아웃). 종료시각 지나면 자동 숨김. 백엔드 공개 GET
  `/api/maintenance` + 관리자 PUT `/api/admin/maintenance`(Firestore config/maintenance).
- `2c36294` **Claude API 장애 자동 감지**(합성 헬스체크): `/api/maintenance`가 폴링에 묶인 능동 핑(1토큰
  Haiku, skip_cache, max_retries=1, 인스턴스별 ≥4분 스로틀, 8초 타임아웃) 수행 → 실패(크레딧/overload/
  rate-limit/timeout/auth) 시 `ai_degraded` → 빨강 'AI 일시 장애' 배너 자동 노출, 복구 시 자동 해제.
  스케줄러 불필요(폴링이 핑 cadence 구동). 자동/수동 독립(수동 우선). /admin에 🟢/🔴 상태 표시.
  검증: 첫 호출 4.83s(핑 발화)·ai_degraded=false·2번째 0.08s(스로틀).

---

## 배포 명령(재현)
- 백엔드: `gcloud builds submit --config=cloudbuild.yaml --project=all-of-asset`
- collector(재수집): `gcloud builds submit --config=cloudbuild-collector.yaml --project=stock-search-program`
  → `gcloud run jobs execute collector-heavy --region=asia-northeast3 --project=stock-search-program --wait`
- 프론트: `web/`에서 `npx vercel --prod --yes`

## 후속 후보
- ETF: 전체 구성종목(KRX 직접) · US 국가/자산 비중 · 보수/추적오차 정렬 스크리닝
- 점검: AI 호출 차단 모드 · 스케줄러 능동핑 · 장애 시 관리자 알림(텔레그램/메일)
- 데이터 수집기 ECOS 날짜포맷 테스트 실패(사전 존재) 정리
