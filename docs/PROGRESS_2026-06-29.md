# 진행 기록 — 2026-06-29

마케팅 생성 품질 고도화 + 주말 브리핑 + 가격/구독/트라이얼 UX + 사이클 밸류에이션 밴드.
모두 main 직접 커밋·prod 배포(백엔드=Cloud Build→Cloud Run `stock-screener`@all-of-asset,
프론트=Vercel `axislytics.com`). 정본 인덱스: [`docs/axis/PROGRESS.md`](axis/PROGRESS.md),
마케팅 정본: [`docs/axis/MARKETING.md`](axis/MARKETING.md).

---

## 1. 마케팅 종목글 생성 품질 고도화

### 1a. 기술 분석 섹션 + 사이클 인지 (커밋 `54d0cf8`)
- **발단(JEON)**: "반도체 호황기엔 현황 PER/PBR만으론 판단 부족" = 경기민감주 **PER 함정**
  (호황 이익피크에 trailing PER이 낮게 찍혀 저평가 착시).
- collector가 이미 계산·저장하던 기술지표(정배열·골든/데드크로스·이격·거래량추세·매집·
  이평수렴·변동성)를 `build_instant_snapshot`/`_snapshot_facts`로 노출 — 글 재료 3개→15개+.
- `_is_cyclical`(반도체·화학·철강·정유·조선·해운 등) + `_cycle_note` → `## 사이클 주의`
  자동 주입. 앵글/작가 프롬프트에 단일 PER 단정 금지 룰.

### 2. 앵글 유형 다양화 (커밋 `54d0cf8`)
- `_ANGLE_SYSTEM`을 '충돌형' 단일재배 → **6종 아키타입**(충돌/사이클위치/추세변곡/수급반전/
  단일충격/과열피로)에서 데이터가 받쳐주는 1개 선택. `PostAngle.archetype` 출력·영속화.

### 3. 연속성 메모리 (커밋 `54d0cf8`)
- `recent_marketing_memory()` — 최근 다룬 종목·과다 사용 앵글 유형 회피
  (`pick_hot_tickers(exclude=)` + `avoid_archetypes`). 라우트·일일잡·CLI잡 3곳 배선.

### 차트 기술 관점 포맷 + 실제 가격 (커밋 `f35ada2`)
- **발단(JEON)**: "기술지표 기반 글포맷이 없다 + 가격을 구체적으로 표현해야".
- `FORMATS`에 `technical`(차트 기술 관점) 추가 — 현재가·이동평균선 '실제 가격(원)'·
  52주 고가/저가를 구체적 숫자로 짚어 차트 국면 관찰. `build_instant_snapshot`에
  ma5/ma20/ma60 실제가 노출, `_technical_facts`가 가격대 먼저 제시(52주 고저=현재가+이격% 역산).
- ⚠️ **목표가/매수가/손절가 금지** — 관찰된 가격 '수준'만 중립 사실로.
- 라이브검증: SK하이닉스 technical = 종가 2,673,000원·52주고 2,919,078원·20일선 2,406,650원
  인용, 금지표현 0건.

### 1b/1c. 밸류에이션 밴드 + EPS 사이클 (커밋 `197f5ec`, 데이터 백필 KRX 주간 대기)
- 현황 단일 PER/PBR을 **자기 역사 밴드** 안에서 보고, **EPS 사이클 위치**(피크/저점)와 결합.
- `screener/core/fundamental_band.py`: `compute_band`(PER/PBR 분위수 + EPS 추세·피크근접도),
  `band_facts`, `cycle_assessment`. 순수 함수 합성검증: 저PER + EPS 사상최고 99% →
  "이익 피크의 낮은 PER 함정" 경고 정확.
- `repository.save/load_fundamental_band` (`fundamental_band/{ticker}`).
- `jobs/backfill_fundamental_band.py`: pykrx `get_market_fundamental_by_date`
  (`[BPS,PER,PBR,EPS,DIV,DPS]`), KR 시총 상위 300×1s 레이트리밋, **KRX 야간점검 시 초반 연속
  빈응답이면 조기 중단**.
- `agents/marketer.py`: KR 종목이면 `## 밸류에이션 밴드` + `## 사이클 판정(밴드 기반)` 주입
  (밴드 없으면 graceful 생략).
- 배포: Cloud Run Job `axis-fundamental-band` + 스케줄러 `0 15 * * 6`(토 15:00 KST,
  야간점검 회피). 잡 1회 실행 검증=정상 완료·야간이라 0건 저장(가드 동작).
- ⚠️ **실제 데이터 백필은 KRX 펀더멘탈 엔드포인트가 살아있는 낮에**: 토 15:00 자동 또는
  `gcloud run jobs execute axis-fundamental-band --region asia-northeast3 --project all-of-asset --wait`.

---

## 2. 주말 결산 브리핑 (커밋 `54d0cf8`)

- 일요일 22:00 KST 발행, 주말 주요소식 + 금요일 미국장 마감 → **다음 거래일(월) 한국장 길잡이**.
  새벽 미국 브리핑(`agents/briefing.py`)의 자매(Sonnet·무슨일/왜/근거·환각금지·순한국어).
- `agents/weekend_briefing.py` + `utils/news_rss.fetch_weekend_news`(주말·다음주 전망) +
  `utils/market_calendar.{next_kr_trading_day,kr_next_session_hint}`.
- `jobs/weekend_briefing.py` + `deploy-weekend-briefing-job.sh` → Cloud Run Job
  `axis-weekend-briefing` + 스케줄러 `0 22 * * 0`. API `POST /weekend-briefing/generate` + 프론트 🗓️ 버튼.
- 라이브 dry-run 검증(27.4원): 금요일 SOXX -5.64% 실데이터 + 주말뉴스 종합 + 월요일 관전.

---

## 3. 가격 · 구독 · 트라이얼 UX

### 가격 인상 (커밋 `b3a87d3`)
- Pro 월 29,000→**39,000**, 연 319,000→**398,000**(연=약 2개월 할인, 월환산 ≈33,200).
- 변경: pricing/CheckoutButton/terms + 백엔드 `pricing.py`(MRR 추정 상수). LS는 **기존 변형
  가격 수정**(변형 ID 불변 → env 무변경). 라이브검증.

### 무료 체험 14일 → 7일 (커밋 `c78da35`)
- 프론트 전체 문구 + 백엔드 `TRIAL_DAYS=7`. ⚠️ 실제 길이는 LS 변형 trial 설정 → **LS에서
  14→7일 변경 완료(JEON)**. 트라이얼 외 14일(ATR·투자자흐름·이벤트 윈도우)·환불 처리기간은 불변.

### '14일 무료' 오해 방지 / trial_eligible (커밋 `a6eb9c7`)
- `GET /api/subscription`에 `trial_eligible`(과거 `lemon_customer_id`·`trial_started` 있으면 false).
- `CheckoutButton`: 이미 Pro→"이미 Pro 이용 중·구독 관리"(결제 CTA 숨김), 트라이얼 소진→
  "Pro 구독 시작"(무료문구 제거), 비로그인/신규→"가입하고 7일 무료로 시작"(디폴트 eligible).

### Pro/Free 배지 + 가입→즉시결제 (커밋 `a22c414`)
- `PlanBadge` — 대시보드 사이드바+모바일 상단바, 접속 직후 플랜 인지(관리자="💎 Pro·관리자").
  구독해지·갱신일 안내는 `SubscriptionSection`에 기존 존재(관리자는 구독 없어 안내메시지만).
- 비로그인 결제 → `/login?next=/pricing?checkout=plan` → 로그인 후 결제 자동 재개.

### 환불 7일 통일 (커밋 `1b416bb`)
- `terms`가 "14일 환불"로 `refund`(7일)와 어긋나던 것 → 7일 통일.

### 대시보드 공통 푸터 (커밋 `d2dda34`)
- 로그인 후에도 요금제·이용약관·개인정보·환불 링크 접근(`SiteFooter`, (dashboard) 레이아웃).

---

## 4. 계정 · 데이터 격리

### 관리자 해지 (Cloud Run 리비전 00114)
- `ADMIN_EMAILS=wogus711929@gmail.com`(messi929@naver.com 제거) → 실고객 테스트용.
  messi929 Firestore 문서는 원래 `tier=free`라 env 변경만으로 충분.

### 최근 분석 유저별 격리 + 로그아웃 누수 수정 (커밋 `ca31a83`)
- 서버: `users/{uid}/analysis_history`·`analysis_runs/{ticker}` — 유저별, 전체 공유 아님.
- 프론트 `recents`(localStorage)가 로그아웃 시 안 지워져 **공유 브라우저 계정 전환 시 누수** →
  `analysisStore.clearOnLogout()` + `signOut`에서 호출.

---

## 5. 남은 일

- **밸류에이션 밴드 첫 데이터 백필** — KRX 주간(낮)에 `axis-fundamental-band` 실행
  (토 15:00 자동 또는 수동). 채워지면 KR 종목글이 밴드 기반 사이클 판정 자동 인용.
- (기존) Threads 앱 bio 면책줄 — JEON 수동(API로 프로필 수정 불가).

---

## 커밋 요약 (시간순)

| 커밋 | 내용 |
|------|------|
| `54d0cf8` | 생성 품질 1a/2/3 + 주말 결산 브리핑 |
| `f35ada2` | 차트 기술 관점 포맷 + 이동평균선·52주 실제 가격 |
| `b3a87d3` | Pro 가격 39,000/398,000 인상 |
| `d2dda34` | 대시보드 공통 푸터(법적 링크) |
| `a22c414` | Pro/Free 배지 + 가입→즉시결제 (+ fundamental_band 코어) |
| `ca31a83` | 로그아웃 시 최근분석 캐시 초기화 |
| `a6eb9c7` | trial_eligible — '14일 무료' 오해 방지 CTA 분기 |
| `c78da35` | 무료 체험 14일 → 7일 |
| `1b416bb` | 환불 기간 7일 통일(terms↔refund) |
| `197f5ec` | 1b/1c 밸류에이션 밴드 백필 + marketer 주입 |
